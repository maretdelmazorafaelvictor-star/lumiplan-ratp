/**
 * Fonctions temps réel (recherche de lignes, import de courses, trafic)
 * s'appuyant sur l'API ecrans-api.gwadz.fr de Leon-ED.
 *
 * Désactivées sur ce déploiement : l'API n'autorise pas notre domaine
 * (CORS). Repasser à true si l'autorisation est accordée un jour —
 * l'accueil et les imports API réapparaîtront tels quels.
 */
export const REALTIME_API_ENABLED = false;
